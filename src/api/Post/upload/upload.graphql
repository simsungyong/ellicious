type Mutation{
    upload(
        caption: String,
        storeName: String!,
        storeLocation: String!, 
        files: [String!], 
        rating: Int!, 
        category: String!, 
        details:[String!]) : Post!
}